{{ define "work_stellerus_swe_2025" }}
\textbf{Software Engineer,} {\small Stellerus Technology} -- Hong Kong SAR \hfill Jan 2025 - Aug 2025 \\
\vspace{-9pt}
\begin{itemize}
    \item Engineered a Pydantic-based DSL and LangGraph execution pipeline to compile unstructured LLM outputs into deterministic tool calls, powering a multi-agent chatbot for complex geospatial operations
    \item Refactored the execution pipeline with a strict schema validation framework, standardizing message payloads to eliminate redundant parsing and cut runtime errors by 30\%
    \item Engineered asynchronous API connectors to bridge the LangGraph agent with the core geospatial backend, optimizing connection pooling and reducing tool execution latency by 120ms
\end{itemize}
{{ end }}
