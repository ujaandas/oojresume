{{ define "work_stellerus_swe_2025" }}
\resumeSubheading
  {Software Engineering Intern}{Jan. 2025 -- Aug. 2025}
  {Stellerus Technology}{Hong Kong SAR}
  \resumeItemListStart
    \resumeItem{Designed a typed execution pipeline in Python to transform unstructured inputs into deterministic tool calls, establishing strict schema guarantees for a complex chatbot environment}
    \resumeItem{Refactored the execution pipeline with strict schema validation, standardizing message payloads to eliminate redundant parsing and reduce runtime errors by 30\%}
    \resumeItem{Bridged this execution engine to the core backend by developing asynchronous API connectors and optimizing connection pooling, reducing task latency by 120ms (35\%)}
  \resumeItemListEnd
{{ end }}
