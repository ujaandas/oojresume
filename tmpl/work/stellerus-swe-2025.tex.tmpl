{{ define "work_stellerus_swe_2025" }}
\textbf{Software Engineer,} {\small Stellerus Technology} -- Hong Kong SAR \hfill Jan 2025 - Aug 2025 \\
\vspace{-9pt}
\begin{itemize}
    \item Designed a JSON-based typed intermediate representation to transform LLM outputs into structured, verifiable commands, enabling the model to act as a predictable, autonomous agent
    \item Built a validation and dispatch framework to ensure agent outputs conformed to the IR schema, enforcing argument types and command structure, improving workflow reliability by \textasciitilde40\%
    \item Refactored the execution pipeline to standardize message formats and remove redundant parsing, supporting safe multi-step agent workflows and reducing runtime errors by \textasciitilde35\%
\end{itemize}
{{ end }}
